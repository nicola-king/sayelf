# Sayelf

Sayelf is a small browser-based conversation workspace. It lets you create separate conversation threads, switch between them, add messages with a role, and keep the data in local browser storage.

## Run locally

```bash
npm start
```

Open <http://localhost:4173> after the server starts.

## Test

```bash
npm test
```
